# Create Your First Project

1. Add `specs/<your-project>.md`.
2. Start with `commands/START-PROJECT.md`.
3. Run `SPEC` to identify gaps and assumptions.
4. Run `PLAN` to generate slices and traceability.
5. Build slice by slice.
6. Test, review, and ship.
